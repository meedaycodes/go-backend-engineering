apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "goapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
